import { ViewNumberPipe } from './view-number.pipe';

describe('ViewNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
